package com.cbfacademy;

import com.FilenameException;

public class FileExtension {
    boolean check(String filename) throws FilenameException {
        if(filename.isEmpty()) {
            throws new FilenameException;
        }
        return false;
    }
}
